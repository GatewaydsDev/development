; PHP-FPM per-directory overrides for the application's public entry point.
; Raises upload limits so avatar/image uploads are not rejected by PHP
; before they reach Laravel validation (which enforces the real cap).
upload_max_filesize = 12M
post_max_size = 16M
